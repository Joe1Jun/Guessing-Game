package ie.atu.sw;



public class Runner {

	public static void main(String[] args) {

		

		new GuessingGame().start();
		//no name on this object

		/*
		 * Anonymous objects are objects that are instantiated without assigning them to
		 * a variable. They are typically used when you only need to use the object once
		 * and there's no need to refer to it later in the code.
		 * 
		 * So, in this case, the object of type GuessingGame is created, its start()
		 * method is called, and since it's an anonymous object, there's no reference to
		 * it after that line of code executes.
		 */

	}
}
